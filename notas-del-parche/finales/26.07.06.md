# Parche 26.07.06

---

## [26.07.06.1] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Soporte de Servidores TURN/STUN para Multijugador P2P**: Se añadió una configuración de `iceServers` en la inicialización de PeerJS, incluyendo servidores STUN de Google y servidores TURN de Open Relay Project. Resuelve errores de tipo `WebRTC: ICE failed` para jugadores con NAT restrictiva (Symmetric NAT/CGNAT).
* **Ajustes de Balance (Buffs y Nerfs)**:
  * **Bomberman (Buff)**: Se redujo drásticamente el cooldown de sus bombas (`bombermanCooldown`) de 94 frames a 45 frames para mayor frecuencia de bombardeo.
  * **Sett (Nerf)**: Se incrementó el cooldown de su habilidad de agarre y azotón (`settCooldown`) de 136 frames a 180 frames para limitar su uso repetitivo.
  * **Terranova (Buff)**: Se aumentó aún más el daño base y escalado de su habilidad especial de muros de tierra (`damage`) a un nuevo rango de `10 - 22` de daño por golpe (anteriormente `8 - 20`, y originalmente `5 - 15`).

### 2. Correcciones de bugs antiguos
* **Estabilidad de Conectividad WebRTC**: Corrección de asimetría en conexiones online de red entre invitados y hosts específicos.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.2] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Optimización de ICE Servers en WebRTC**: Se redujo y depuró la lista de servidores STUN y TURN configurados en PeerJS. Esto elimina la advertencia de Firefox *"Using five or more STUN/TURN servers slows down discovery"* que causaba cuellos de botella y ralentizaciones en la recolección de candidatos de red.
* **Soporte TLS para TURN (HTTPS)**: Se implementó la URL con protocolo seguro `turns:openrelay.metered.ca:443?transport=tcp` para asegurar la compatibilidad con navegadores modernos y evitar bloqueos por políticas de contenido mixto (Mixed Content) al hospedar el juego sobre HTTPS (GitHub Pages).

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.3] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Compresión Extrema del Estado del Jugador (Tuplas)**: Se migró la serialización del estado del jugador de un objeto JSON con 46 propiedades repetidas a una estructura plana de tuplas (arrays indexados). Esto reduce el tamaño de los paquetes de estado de red en un **~75%**, aliviando drásticamente el consumo de ancho de banda y previniendo la fragmentación de paquetes en WebRTC.
* **Envío Inteligente de Inputs (Control de Cambios)**: El navegador del Invitado (Guest) ahora solo transmite información de controles (`guest_inputs`) al Host cuando hay un cambio real en el estado de las teclas (presionar o soltar). Si no hay cambios, no se envía ningún paquete. Esto reduce el tráfico de subida del invitado hasta en un **90%** durante el juego pasivo o estacionario.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.4] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Generación Dinámica de Credenciales TURN (HMAC-SHA1)**: Se implementó un generador dinámico de credenciales en el cliente (usando la API nativa `window.crypto.subtle`) a partir del secreto compartido de Open Relay (`openrelayprojectsecret`). Esto resuelve de raíz el error de autenticación del servidor TURN (`your TURN server appears to be broken`), permitiendo que el servidor de relevo acepte conexiones y actúe como puente cuando el P2P directo falla.

### 2. Correcciones de bugs antiguos
* **Conectividad en la Misma Red (NAT Loopback)**: Resuelto el fallo de conexión de dispositivos en el mismo Wi-Fi que fallaban por bloqueos de NAT Hairpinning/mDNS locales, gracias a la activación exitosa del canal de relevo TURN dinámico.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.5] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Suite de Depuración WebRTC en Consola**: Se implementó un completo kit de monitorización de estados WebRTC (`debugConnection` y `monitorRTCPeerConnection`). Esto escribe en la consola del navegador todos los cambios de estados de la conexión (`connectionState`), negociación ICE (`iceConnectionState`, `iceGatheringState`), listado de candidatos ICE locales descubiertos y los SDP (Session Description Protocol) locales y remotos en caso de fallo, permitiendo un diagnóstico exhaustivo.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.6] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Registro Detallado de ICE Servers**: Se mejoró el depurador en consola de `initMultiplayer` para serializar la configuración completa de `iceServers` en formato JSON legible. Esto permite inspeccionar la validez sintáctica y los valores de las credenciales dinámicas generadas para el servidor TURN.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.7] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Soporte para Servidor TURN Personalizado**: Se implementó una sección de configuración de Servidor TURN Personalizado en el panel de Opciones. Los usuarios pueden introducir su propia URL de servidor TURN, usuario y contraseña (útil para redes restrictivas detrás de firewalls o CGNAT). Los datos se almacenan de forma persistente en `localStorage`.
* **Redundancia de Servidores STUN**: Se expandió el listado de servidores STUN de Google predeterminados (`stun1` a `stun4`) para dar mayor redundancia y asegurar el descubrimiento de candidatos en diferentes puertos.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.8] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Aplanamiento de la Estructura de ICE Servers (Parser Fix)**: Se migró la configuración de `iceServers` de arrays de múltiples URLs por objeto a objetos planos individuales con una sola URL (`urls: 'string'`). Esto soluciona un bug crítico en algunos parsers internos de PeerJS y en ciertos motores de navegadores que descartaban o abortaban la recolección de candidatos ICE (recogiendo 0 candidatos) cuando recibían arrays de URLs para servidores STUN/TURN.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.09] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Downgrade a PeerJS 1.4.7**: Reemplazada la versión 1.5.4 de PeerJS por la 1.4.7 para resolver fallos críticos en la retención de sockets y reconexión en dispositivos móviles.
* **Redundancia y Robustez de Servidores TURN**: Se corrigió el nombre de usuario dinámico generado y se añadieron puertos/protocolos redundantes (UDP/TCP en 80/443), incluyendo credenciales estáticas de respaldo (`openrelayproject`) para garantizar conectividad TURN en cualquier red.
* **Depuración de Señalización**: Forzada la configuración explícita del host de señalización y activado `debug: 3` en consola.

### 2. Correcciones de bugs antiguos
* **Prevención de Conexiones Prematuras**: Se implementó una verificación de estado de registro en el servidor de señales (`peer.open`) antes de conectar a sala para evitar fallos de conexión silenciosos por clicks rápidos del usuario.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.10] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Soporte para Sobrescritura de Peer ID en Servidor de Señales**: Se resolvió de raíz el problema donde el servidor PeerJS ignora o sobrescribe la petición de ID personalizado (`smashturbanda-XXXX`) debido a cuotas o colisiones silenciosas. El sistema ahora detecta dinámicamente si el servidor asignó un ID aleatorio (UUID) y lo muestra en el UI como el código de sala real, permitiendo a los clientes unirse ingresando dicho ID directamente.
* **Conexión Flexibilizada**: Se modificó el campo de unirse a sala para aceptar tanto el código numérico de 4 dígitos como el UUID completo asignado por el servidor de señales.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.11] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Estructura Array-of-Strings para ICE Servers**: Se reestructuró la definición de `iceServers` en `multiplayer.js` para usar la propiedad `urls` como un array de strings (cumpliendo estrictamente la especificación WebRTC standard `sequence<DOMString>`). Esto soluciona de raíz el fallo de recolección de candidatos locales en navegadores específicos que abortaban silenciosamente el proceso de ICE gathering al recibir URLs individuales en formato plano de strings.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.07.06.12] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Remoción de Servidores TURN Públicos Obsoletos (Default STUN)**: Se eliminó la configuración predeterminada de los servidores TURN de Open Relay (`openrelay.metered.ca`) del código base. Al estar caídos o bloqueados públicamente, generaban fallos de autenticación críticos (`WebRTC: ICE failed, your TURN server appears to be broken`) que provocaban que los navegadores abortaran la fase de emparejamiento de candidatos ICE (impidiendo incluso las conexiones directas STUN viables entre redes domésticas). El juego ahora opera por defecto únicamente con los servidores STUN oficiales y estables de Google.

### 2. Correcciones de bugs antiguos
* Ninguno.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.




