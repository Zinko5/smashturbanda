# Parche 26.08.01

---

## [26.08.01.01] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Robustez en Validación de Versiones**: Se mejoró el controlador `version_check` para retrasar la desconexión 3.5 segundos en caso de incompatibilidad, permitiendo que los usuarios visualicen el aviso informativo de versión. Asimismo, se implementó un refrescado forzado (`window.location.reload(true)`) en ambos extremos (host y guest) para limpiar automáticamente cachés persistentes del navegador.

### 2. Correcciones de bugs antiguos
* **Desincronización de Versión en multiplayer.js**: Se corrigió el bug que congelaba la constante interna `GAME_VERSION` en la versión anterior (`26.07.06.12`) mientras el archivo HTML mostraba la `.13`. Este mismatch provocaba desconexiones silenciosas inmediatas en la fase de emparejamiento.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.08.01.04] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Soporte de API Key Segura para TURN**: El intento de usar el Open Relay estático de Metered.ca fue revertido porque el servicio rechaza conexiones públicas, fallando silenciosamente. En su lugar, se implementó una constante `TURN_BACKEND_URL` en `multiplayer.js`. Esto permite al desarrollador usar un proxy serverless (como Cloudflare Workers) que oculta la API Key de Metered y distribuye credenciales dinámicas de TURN a los jugadores de forma invisible y segura.
* **Limpieza de UI de TURN**: Se removió el campo de API Key del menú Opciones, dejando solo la configuración avanzada de TURN personalizado para usuarios con servidores propios.

### 2. Correcciones de bugs antiguos
* **Conexión WebRTC Fallida (ICE Failed) entre Redes Distintas**: Resuelto el fallo de conexión entre dispositivos en redes diferentes (ej. WiFi ↔ datos móviles) que fallaban con error `ICE failed` al no existir servidores TURN de respaldo. La causa raíz era que en el parche `26.07.06.12` se habían eliminado los servidores TURN por problemas de formato de URLs, dejando el juego sin capacidad de relevo.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.08.01.06] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Proxy de Cloudflare Definitivo**: Se reestructuró el Worker de Cloudflare para utilizar directamente una `API Key` permanente mediante una petición `GET` a Metered.ca. Esto evita por completo los bloqueos por límite de peticiones (Rate Limit 429/500) que ocurrían al generar múltiples contraseñas temporales mediante `POST` durante las pruebas de recarga de página.
* **Fallback a OpenRelay**: Se re-inyectó el servidor gratuito `openrelay.metered.ca` como última línea de defensa en el array de `iceServers`.

### 2. Correcciones de bugs antiguos
* **Diagnóstico de Bloqueo de Red (ISP)**: Se documentó exhaustivamente que el estado infinito de "conectando..." (ICE Failed silencioso) al jugar entre celular (datos móviles) y laptop no es un bug del código WebRTC ni de PeerJS. Las pruebas revelaron que la conexión fracasa también estando ambos dispositivos en la misma red Wi-Fi si el router tiene habilitado "AP Isolation" (Aislamiento de Clientes), o si el proveedor de telefonía móvil bloquea el protocolo UDP y WebRTC mediante Deep Packet Inspection (DPI) (ej. Telcel, Movistar, Claro) para impedir llamadas VoIP gratuitas. Ante estas redes hostiles (Symmetric NAT con bloqueo DPI), el estándar WebRTC P2P es bloqueado físicamente. **Solución:** Jugar en redes Wi-Fi residenciales estándar sin restricciones, o utilizar una VPN en el dispositivo móvil para evadir el bloqueo de la operadora.

### 3. Correcciones de bugs que vinieron con las nuevas características
* **Procesamiento de ICE Servers**: El parsing del JSON devuelto por Metered en `multiplayer.js` se refinó para extraer de manera limpia todas las URLs proporcionadas (UDP, TCP, TLS en los puertos 80, 443, 3478) manteniendo la última credencial activa, aumentando dramáticamente la resiliencia en redes que no bloquean P2P pero que sí filtran puertos estándar.

---

## [26.08.01.07] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Soporte Táctil y Responsividad Extrema**: Se implementaron controles táctiles virtuales en pantalla (D-Pad y botones de acción) para usuarios en dispositivos móviles. Los controles envían comandos directamente al jugador P1 local. 
* **Responsividad UI Dinámica**: Se incorporaron media queries (`@media`) agresivas en todo el sistema de CSS (`styles.css`) para compactar y achicar de manera proporcionada las tarjetas de selección de personaje, menús y grids, impidiendo que el contenido quede fuera de la pantalla (overflow) en celulares de baja resolución o pantallas pequeñas.
* **Solicitud de Orientación (Landscape)**: Se agregó un overlay especial que se activa cuando un jugador ingresa a la página desde un móvil en modo retrato (Portrait). Este aviso interrumpe la interacción invitando al usuario a rotar su dispositivo y un botón solicitará la API de Orientación para forzar la pantalla completa y el modo horizontal.

### 2. Correcciones de bugs antiguos
* Ninguno detectado.

### 3. Correcciones de bugs que vinieron con las nuevas características
* Ninguno detectado.

---

## [26.08.01.08] - Miniparche de Desarrollo

### 1. Nuevas características o funciones
* **Sustitución de Botón D-Pad por OBJ (Objetos)**: Se reemplazó la flecha redundantemente asignada al salto "Arriba" en el D-Pad por un botón dedicado **OBJ** (`grab`) en los controles móviles de la derecha, permitiendo activar Pumas y objetos especiales rápidamente desde celulares.
* **Reposicionamiento de HUD Móvil**: Las barras de daño (%) y vidas (`#game-hud`) se reubicaron en la parte superior de la pantalla (`top: 8px`) con escala reducida durante partidas móviles para evitar obstruir el escenario o los botones virtuales.

### 2. Correcciones de bugs antiguos
* **Scroll y Recorte en Menús Móviles Horizontal (Landscape)**: Se corrigió un error que cortaba verticalmente las tarjetas de menú (`.menu-card` y `.overlay`) sin permitir scroll al jugar en modo horizontal en pantallas cortas. Se agregó `overflow-y: auto`, reducción de padding e iteración adaptativa en viewports reducidos (`max-height: 500px`).
* **Incompatibilidad de Guía de Teclas PC en Móviles**: Se ocultó de forma nativa la caja informativa de controles de teclado (`#game-controls-overlay`) en pantallas táctiles/móviles para liberar espacio sobre el D-Pad.

### 3. Correcciones de bugs que vinieron con las nuevas características
* **Visualización Dinámica del Cartel de Rotación**: Se ajustó la alerta de rotación (`#mobile-rotation-prompt`) para ocultarse de manera reactiva en cuanto el sensor de orientación / viewport cambia a modo apaisado (landscape), manteniendo la compatibilidad con el botón de Pantalla Completa nativo.

